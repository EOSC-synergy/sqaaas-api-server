{
  "name": "{{ pipeline_name }}",
  "config_data": [
    {
      "config": {
        "project_repos": [
	  {%- for repo in [repo_code, repo_docs, repo_deploy] -%}
          {%- if repo.get('repo', {})  -%}
	  {{ "," if not loop.first else "" }}
          {
            "repo": "{{ repo.repo }}",
            "branch": "{{ repo.branch }}"
	  }
	  {%- endif -%}
	  {%- endfor -%}
        ],
        "credentials": []
      },
      "sqa_criteria": {
        {%- for criterion_data in criteria_data_list %}
        "{{ criterion_data.id }}": {
          "repos": [
            {%- for tool in criterion_data.tools + criterion_data.get(tooling_qaa_specific_key, []) %}
            {
              {%- if criterion_data.id in ["QC.Doc"] and repo_docs.repo %}
              "repo_url": "{{ repo_docs.repo }}",
              {%- elif criterion_data.id in ["SvcQC.Dep"] and repo_deploy.repo %}
              "repo_url": "{{ repo_deploy.repo }}",
              {%- else %}
              "repo_url": "{{ repo_code.repo }}",
              {%- endif %}
              "container": "",
              "tool": {{ tool | tojson }}
            }{{"," if not loop.last}}
            {%- endfor %}
          ]
        }{{"," if not loop.last}}
        {%- endfor %}
      },
      "environment": {
          "JPL_KEEPGOING": "enabled"
      },
      "timeout": 1800
    }
  ],
  "composer_data": {
    "version": "3.7",
    "services": {}
  },
  "jenkinsfile_data": {}
}
